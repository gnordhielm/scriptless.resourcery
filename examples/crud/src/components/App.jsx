import React from 'react'
import { ResourceProvider } from '@scriptless/resourcery'

const App = () => (
  <ResourceProvider>
    <div className="App">App</div>
  </ResourceProvider>
)

export default App
